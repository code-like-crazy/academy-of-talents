/*
 * Auto-generated by: https://github.com/pmndrs/gltfjsx
 * Component renders a 3D beech tree model using react-three-fiber
 */

import React, { JSX, useRef } from "react";
import { useGLTF } from "@react-three/drei";
import * as THREE from "three";
import { GLTF } from "three-stdlib";

const MODEL_URL =
  "https://vazxmixjsiawhamofees.supabase.co/storage/v1/object/public/models/tree-beech/model.gltf";

type GLTFResult = GLTF & {
  nodes: {
    "tree-beech": THREE.Mesh;
  };
  materials: {
    color_main: THREE.MeshStandardMaterial;
  };
};

export default function TreeBeech(props: JSX.IntrinsicElements["group"]) {
  const group = useRef<THREE.Group>(null);
  const { nodes, materials } = useGLTF(
    MODEL_URL,
    true,
  ) as unknown as GLTFResult;

  return (
    <group ref={group} {...props} dispose={null}>
      <mesh
        geometry={nodes["tree-beech"].geometry}
        material={materials.color_main}
      />
    </group>
  );
}

useGLTF.preload(MODEL_URL);
