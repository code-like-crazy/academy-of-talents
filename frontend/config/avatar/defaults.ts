export const DEFAULT_BLINK_SETTINGS = {
  minInterval: 1000,
  maxInterval: 5000,
  duration: 200,
};

// Speed of morphing transitions - higher means faster transitions
export const DEFAULT_MORPH_TARGET_LERP_SPEED = 0.3;
export const DEFAULT_BLINK_LERP_SPEED = 0.5;
export const DEFAULT_ANIMATION_FADE_DURATION = 0.5;
